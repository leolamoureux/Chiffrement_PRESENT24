#include "attaque.h"
#include "PRESENT24.h"
/*2^24=16777216*/
void comparaison_listes(unsigned int message_clair[24], unsigned int message_chiffre[24]){
    for(int i=0;i<5;i++){
        /*cadencement pour chaque clé i */
        /*chiffrement message clair avec le cadencement*/
        for(int j=0;j<5;j++){
            /*cadencement pour la clé j*/
            /*chiffrement(message_chiffré,cadencement)*/
            /*comparaison messageclair(i) avec tous les message chiffrés*/
        }
    }
}
